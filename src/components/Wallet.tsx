import { ConnectButton } from "@rainbow-me/rainbowkit";

//TODO: use wallet for portfolio
const Wallet = () => {
  // return <ConnectButton showBalance={false} chainStatus={"none"} />;
  return <div />;
};

export default Wallet;
